import React from "react";

const LayoutAuthentication = (props) => {
  const { children } = props;
  return <div className="w-full min-h-screen bg-lite"></div>;
};

export default LayoutAuthentication;
